<%= form_with model: @transaction, path: category_transactions_path,method: :post do |form| %>
  <% if @transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% @transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
      <%= form.label :category, style: "display: block" %>
      <%= form.select:category_id, options_for_select(@categories),{include_blank: "Select Category"},{required: true, class: "form-select "} %>   
   </div>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amouunt %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
